PORT=5000
MONGO_URI=mongodb://localhost:27017/ims
# Must be a replica set (even single-node) for multi-document transactions.
# Local dev: run `mongod --replSet rs0`, then once: `mongosh --eval "rs.initiate()"`
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=8h